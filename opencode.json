{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "agent": {
    "code-reviewer": {
      "description": "Reviews Next.js/TypeScript code for bugs, quality issues, code smells, type errors, and unused code.",
      "mode": "subagent",
      "permission": { "edit": "deny", "bash": "ask" }
    },
    "security-reviewer": {
      "description": "Reviews the app for security vulnerabilities — XSS, injection, data exposure, API key handling, and unsafe external inputs.",
      "mode": "subagent",
      "permission": { "edit": "deny", "bash": "ask" }
    }
  }
}
